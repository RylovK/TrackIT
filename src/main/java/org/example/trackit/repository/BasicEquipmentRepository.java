package org.example.trackit.repository;

import org.example.trackit.entity.BasicEquipment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BasicEquipmentRepository extends JpaRepository<BasicEquipment, Integer> {
}
